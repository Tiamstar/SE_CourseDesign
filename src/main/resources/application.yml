server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/se_assistant?useSSL=false&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  redis:
    host: localhost
    port: 6379
    database: 0

logging:
  level:
    root: INFO
    com.example.assistant: DEBUG

milvus:
  host: localhost
  port: 19530 